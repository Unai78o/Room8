import { LucideIcon } from "lucide-react";

interface Props {
    icon: LucideIcon;
    isActive?: boolean;
}

export function SidebarButton({ icon: Icon, isActive = false }: Props) {
    return (
        <div className="relative flex items-center justify-center group">
            
        </div>
    );
}